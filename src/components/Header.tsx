import { FC } from "react";

const Header: FC = () => {
  return (
    <header className="flex justify-center p-4 m-4 border-b-black border-b">
      <h1 className="text-4xl font-bold">Todo App</h1>
    </header>
  );
};

export default Header;
